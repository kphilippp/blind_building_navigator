/**
 * Configuration file template
 *
 * To use this file:
 * 1. Copy this file and rename it to "config.ts"
 * 2. Add your OpenAI API key to enable real speech-to-text
 * 3. Get an API key from: https://platform.openai.com/api-keys
 *
 * Note: config.ts is gitignored to keep your API key secure
 */

export const config = {
  // OpenAI API key for Whisper speech-to-text
  // Leave empty to use mock data (random building/room names)
  openAiApiKey: "",

  // Other settings can go here
};
